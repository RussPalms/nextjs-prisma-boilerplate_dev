
APP_ENV=replit

# ------------------
# replit

# http - use http server
SITE_PROTOCOL=http

# only backend will use this port (http server)
PORT=42069

SITE_HOSTNAME=nextjs-prisma-boilerplate-node16_dev.russpalms.repl.co

# no port in url
#NEXTAUTH_URL=https://nextjs-prisma-boilerplate-node16_dev.russpalms.repl.co
NEXTAUTH_URL=https://${SITE_HOSTNAME}

# https://replit.com/russpalms/nextjs-prisma-boilerplate-node16_dev#package.json

# -----------------
# examples

#https://nextjs-prisma-boilerplate-node16_dev.russpalms.repl.co

# workspace
#https://replit.com/russpalms/nextjs-prisma-boilerplate-node16_dev


# private vars (postgres and app containers) ----------------------------------

# db container
POSTGRES_HOSTNAME=192.168.4.200
POSTGRES_PORT=5432
POSTGRES_USER=rpalm_user
POSTGRES_PASSWORD=
POSTGRES_DB=npb-db-live

# app container, used in schema.prisma, expand it immediately
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOSTNAME}:${POSTGRES_PORT}/${POSTGRES_DB}?schema=public

# current host user as non-root user in db container, set it here
MY_UID=1001
MY_GID=1001

# or better globally in ~/.bashrc
# export MY_UID=$(id -u)
# export MY_GID=$(id -g)

# -------

# app container, next-auth vars

# jwt secret
SECRET=RANDOM_STRING

# Facebook
FACEBOOK_CLIENT_ID=
FACEBOOK_CLIENT_SECRET=

# Google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=




# private vars (postgres and app containers) ----------------------------------

# db container
POSTGRES_HOSTNAME=192.168.4.200
POSTGRES_PORT=5432
POSTGRES_USER=rpalm_user
POSTGRES_PASSWORD=
POSTGRES_DB=npb-db-live

# app container, used in schema.prisma, expand it immediately
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOSTNAME}:${POSTGRES_PORT}/${POSTGRES_DB}?schema=public

# current host user as non-root user in db container, set it here
MY_UID=1001
MY_GID=1001

# or better globally in ~/.bashrc
# export MY_UID=$(id -u)
# export MY_GID=$(id -g)

# -------

# app container, next-auth vars

# jwt secret
SECRET=RANDOM_STRING

# Facebook
FACEBOOK_CLIENT_ID=
FACEBOOK_CLIENT_SECRET=

# Google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

SITE_HOSTNAME=${PORT}-${HOSTNAME}.${GITPOD_WORKSPACE_CLUSTER_HOST}


# no port in url
#NEXTAUTH_URL=https://42069-russpalms-nextjsprismabo-h4e9fmpo6a5.ws-eu47.gitpod.io
NEXTAUTH_URL=https://${PORT}-${HOSTNAME}.${GITPOD_WORKSPACE_CLUSTER_HOST}


# -----------------
# examples

# both website and api are on https without port
# Gitpod makes some proxy

# site:
# https://42069-russpalms-nextjsprismabo-h4e9fmpo6a5.ws-eu47.gitpod.io/users/
# api:
# https://42069-russpalms-nextjsprismabo-h4e9fmpo6a5.ws-eu47.gitpod.io/api/users/?page=1

# existing gitpod env vars
#GITPOD_WORKSPACE_CLUSTER_HOST=ws-eu47.gitpod.io
#HOSTNAME=russpalms-nextjsprismabo-h4e9fmpo6a5

# workspace, without 42069-
#GITPOD_WORKSPACE_URL=https://russpalms-nextjsprismabo-h4e9fmpo6a5.ws-eu47.gitpod.io
# private vars (postgres and app containers) ----------------------------------

# db container
POSTGRES_HOSTNAME=192.168.4.200
POSTGRES_PORT=5432
POSTGRES_USER=rpalm_user
POSTGRES_PASSWORD=
POSTGRES_DB=npb-db-live

# app container, used in schema.prisma, expand it immediately
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOSTNAME}:${POSTGRES_PORT}/${POSTGRES_DB}?schema=public

# current host user as non-root user in db container, set it here
MY_UID=1001
MY_GID=1001

# or better globally in ~/.bashrc
# export MY_UID=$(id -u)
# export MY_GID=$(id -g)

# -------

# app container, next-auth vars

# jwt secret
SECRET=RANDOM_STRING

# Facebook
FACEBOOK_CLIENT_ID=
FACEBOOK_CLIENT_SECRET=

# Google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

SITE_HOSTNAME=${PORT}-${HOSTNAME}.${GITPOD_WORKSPACE_CLUSTER_HOST}


# no port in url
#NEXTAUTH_URL=https://42069-russpalms-nextjsprismabo-h4e9fmpo6a5.ws-eu47.gitpod.io
NEXTAUTH_URL=https://${PORT}-${HOSTNAME}.${GITPOD_WORKSPACE_CLUSTER_HOST}


# -----------------
# examples

# both website and api are on https without port
# Gitpod makes some proxy

# site:
# https://42069-russpalms-nextjsprismabo-h4e9fmpo6a5.ws-eu47.gitpod.io/users/
# api:
# https://42069-russpalms-nextjsprismabo-h4e9fmpo6a5.ws-eu47.gitpod.io/api/users/?page=1

# existing gitpod env vars
#GITPOD_WORKSPACE_CLUSTER_HOST=ws-eu47.gitpod.io
#HOSTNAME=russpalms-nextjsprismabo-h4e9fmpo6a5

# workspace, without 42069-
#GITPOD_WORKSPACE_URL=https://russpalms-nextjsprismabo-h4e9fmpo6a5.ws-eu47.gitpod.io
# private vars (postgres and app containers) ----------------------------------

# db container
POSTGRES_HOSTNAME=192.168.4.200
POSTGRES_PORT=5432
POSTGRES_USER=rpalm_user
POSTGRES_PASSWORD=
POSTGRES_DB=npb-db-live

# app container, used in schema.prisma, expand it immediately
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOSTNAME}:${POSTGRES_PORT}/${POSTGRES_DB}?schema=public

# current host user as non-root user in db container, set it here
MY_UID=1001
MY_GID=1001

# or better globally in ~/.bashrc
# export MY_UID=$(id -u)
# export MY_GID=$(id -g)

# -------

# app container, next-auth vars

# jwt secret
SECRET=RANDOM_STRING

# Facebook
FACEBOOK_CLIENT_ID=
FACEBOOK_CLIENT_SECRET=

# Google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=


# private vars (postgres and app containers) ----------------------------------

# db container
POSTGRES_HOSTNAME=192.168.4.200
POSTGRES_PORT=5432
POSTGRES_USER=rpalm_user
POSTGRES_PASSWORD=
POSTGRES_DB=npb-db-live

# app container, used in schema.prisma, expand it immediately
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOSTNAME}:${POSTGRES_PORT}/${POSTGRES_DB}?schema=public

# current host user as non-root user in db container, set it here
MY_UID=1001
MY_GID=1001

# or better globally in ~/.bashrc
# export MY_UID=$(id -u)
# export MY_GID=$(id -g)

# -------

# app container, next-auth vars

# jwt secret
SECRET=RANDOM_STRING

# Facebook
FACEBOOK_CLIENT_ID=
FACEBOOK_CLIENT_SECRET=

# Google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# export MY_GID=$(id -g)

# -------

# app container, next-auth vars

# jwt secret
SECRET=RANDOM_STRING

# Facebook
FACEBOOK_CLIENT_ID=
FACEBOOK_CLIENT_SECRET=

# Google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

